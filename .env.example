# Production Environment Variables for Streaming Deployment

# Application
NODE_ENV=production
PORT=5000

# Database Configuration (for Docker Compose deployment)
POSTGRES_DB=snakedb
POSTGRES_USER=snake_user
POSTGRES_PASSWORD=your_secure_database_password_here

# Streaming Configuration
RTMP_URL=rtmps://pump-prod-tg2x8veh.rtmp.livekit.cloud/x/uTHga4yGUYXW
SNAKE_GAME_URL=https://bioqz.com

# Database URL (for direct connection)
DATABASE_URL=postgresql://username:password@host:port/database

# Production Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. Use strong, unique passwords for production
# 4. For Docker deployment, set POSTGRES_* variables
# 5. For direct deployment, set DATABASE_URL